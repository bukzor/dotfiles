#!/bin/bash
# Ensure object store is set up on first index operation (git add)

set -euo pipefail

HERE=$(cd "$(dirname "$0")"; pwd)

# Check if .git is still a directory (not yet converted to worktree)
if [ -d .git ]; then
    # Run the setup
    "$HERE/post-init"
fi
