# configuration file for readline (aka "tab completion")

# Use a flash instead of a ding.
set bell-style visible

# this is from /etc/inputrc, but they only enabled it for emacs mode:
"\e[1~": beginning-of-line
"\e[4~": end-of-line
"\e[3~": delete-char
"\e[2~": quoted-insert
