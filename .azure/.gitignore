# Opt-out model: everything under .azure/ is tracked by default.
# Add secrets and transient state here.

# Tokens / caches — NEVER VCS
accessTokens.json
msal_token_cache.*
service_principal_entries.bin

# Transient
logs/
telemetry.txt
commandIndex.json
