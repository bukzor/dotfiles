---
anthropic-skill-ownership: llm-subtask
---

1. [ ] Disable unused providers: `let g:loaded_{perl,ruby,node}_provider = 0` in vim init. Install python provider via `( cd ~ && uv add pynvim )`.
2. [ ] Migrate `folke/neodev.nvim` → `folke/lazydev.nvim` (neodev deprecated per neoconf checkhealth).
