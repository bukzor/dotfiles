#!/bin/bash
# Encode a path using Claude Code's encoding scheme
# Usage: claude-path [PATH]
#   Encodes PATH (or PWD if not provided)

set -euo pipefail

path="${1:-$PWD}"
path=$(realpath -Lm "$path")

# Encoding: - → --, then / → -
sed -e 's/-/--/g' -e 's/\//-/g' <<<"$path"
