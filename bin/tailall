#!/bin/bash

tail -fn0 logs/apperror.log | pretty_error_log &
tail -fn0 logs/app.log &

pgname=`python -c 'import config.custom; print config.custom.pgname'`
tail -fn0 ~/.pg-$pgname/logs/error.log &

