#!/usr/bin/python
from __future__ import print_function
from sys import argv
from pipes import quote
quoted = ' '.join([quote(arg) for arg in argv[1:]])

# Make the string slightly shorter by factoring out noop '' sequences
quoted = quoted.replace("''", "")

print(quoted)
