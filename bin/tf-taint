#!/bin/bash
set -euo pipefail

for id in "$@"; do
  echo "$id" |
    sed -r '
      s/^(.*)\.([^.]*\.[^.]*)$/tf taint --module \1 \2/
      s/module\.//g
      s/\[/./g
      s/\]//g
    ' |
    sh -x
done
