" search
set hlsearch            "highlight matches
set incsearch           "show matches while the pattern is typed
set ignorecase          "match even if captilization doessn't
set smartcase           "unless the pattern isn't just lowercase
