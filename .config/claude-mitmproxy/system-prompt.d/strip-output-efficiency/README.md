Strip the "Output efficiency" section from the system prompt.

This section directly contradicts user CLAUDE.md instructions to give
reasoning before conclusions. It pushes toward shallow, answer-first
responses and suppresses the reasoning that makes output trustworthy.
