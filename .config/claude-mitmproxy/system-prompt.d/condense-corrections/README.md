Condense `# Corrections` (opus-5 shape, v2.1.221+).

Two paragraphs, ~250 words, restating "correct errors that matter,
don't ruminate" seven ways. The replacement keeps each distinct rule
(materiality threshold, plain delivery, silent trivial fixes, agent
skepticism, follow-up != error, thinking-block exemption) in ~70 words.

Opinion rewrite, not drift repair: `search.md` is the verbatim section
body, so any upstream edit is a loud search miss prompting a
re-condense against the new text.
