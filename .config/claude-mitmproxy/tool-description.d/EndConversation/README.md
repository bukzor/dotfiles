Largest built-in description served (~4.5k chars) for a tool that is
essentially never used. The stub keeps the hard constraints inline
(last-resort-only, warning-first, self-harm exclusions, fork no-op) and
defers the full escalation ladder to the must-read entry, which the stub
requires reading before any use -- the one moment the policy is relevant.
