# Opt-out model: everything under .aws/ is tracked by default.
# Add secrets and transient state here.

# Static credentials — NEVER VCS
credentials

# SSO / STS token caches — NEVER VCS
sso/cache/
cli/cache/
boto/cache/
