---
requires:
  - ~/.claude/reference.kb/git/conventions.md
---

# Before any git command

Conventions — `git -C`/path scoping, the `git l` alias, index hygiene — are
authoritative in the required reference. ALL git commands MUST include explicit
path arguments; unqualified commands are blocked.

For commits, see `commit.md`.
