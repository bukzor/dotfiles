# Markdown style

## Skip bold

No `**bold**` -- plain text carries emphasis via word choice and structure.

## Use `--`, not em-dash

Write a literal double-dash (`--`) for a parenthetical break, not `—`.
