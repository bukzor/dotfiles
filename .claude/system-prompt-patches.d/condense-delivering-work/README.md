Condense `# Delivering work` (opus-5 shape, v2.1.221+).

Three paragraphs, ~450 words, restating "do what was asked, don't
rescope, finish it, flag concerns once" five or six ways -- accreted
patch-on-patch prose, not an edited section. The replacement keeps
every distinct instruction (scope fidelity, routine judgment calls,
flag-then-build, finish-or-report, uncertainty ordering, reaffirmation,
refusal shape) in ~130 words.

This is an opinion rewrite, not drift repair: `search.md` is the
verbatim section body, so any upstream edit to it is a loud search miss
prompting a re-condense against the new text.
