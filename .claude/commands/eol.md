Please ensure every file ends with a newline.
Note that the parser of the antml xml strips the final newline from parameter text nodes, so you must add an additional newline, *two* trailing newlines, to represent a trailing newline.
