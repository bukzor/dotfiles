# Count data variants

The complexity metric for a design is how many distinct shapes its data can
take. Reduce that count as far as possible without breaking a use case.

Corollary: bending one format to serve two use cases normally beats keeping
two formats.

Diagnostic: a second format, schema, or mode is being added "for" a new use
case — first try adjusting the existing one to serve both.
