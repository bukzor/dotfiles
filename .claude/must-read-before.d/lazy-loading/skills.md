# Lazy-Loaded Skills

Skills in `~/.claude/skill-categories/` are not loaded by default to save token overhead.

## Usage

When a task requires a lazy-loaded skill:

1. List available skills in the category: `ls ~/.claude/skill-categories/CATEGORY/`
2. Explore the appropriate skill: `ls -R ~/.claude/skill-categories/CATEGORY/SKILLNAME/`
